@* @model IEnumerable<PIBitacoras.Models.BitacoraListadoVM>

@{
    ViewData["Title"] = "Bitácoras Clínicas";
}

<h2 class="mb-4">BITÁCORAS CLÍNICAS</h2>

<a asp-action="Create" class="btn btn-dark mb-4">NUEVA BITÁCORA</a>

<table class="table table-hover align-middle">
    <thead class="table-light">
        <tr>
            <th>Matrícula</th>
            <th>Alumno</th>
            <th>Motivo</th>
            <th>Estado</th>
            <th>Folio</th>
            <th style="width:180px;">Acciones</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Matricula</td>
                <td>@item.NombreCompleto</td>
                <td>@item.Motivo</td>

                <td>
                    @if (item.Estado == "Estable")
                    {
                        <span class="badge bg-success">Estable</span>
                    }
                    else if (item.Estado == "Urgente")
                    {
                        <span class="badge bg-danger">Urgente</span>
                    }
                    else
                    {
                        <span class="badge bg-secondary">@item.Estado</span>
                    }
                </td>

                <td><strong>@item.Folio</strong></td>

                <td>
                    <div class="d-flex gap-2">

                        <a asp-action="Details" asp-route-id="@item.Id"
                           class="btn btn-sm btn-primary">
                            Ver
                        </a>

                        <a asp-action="Edit" asp-route-id="@item.Id"
                           class="btn btn-sm btn-warning text-white">
                            Editar
                        </a>

                        <a asp-action="Delete" asp-route-id="@item.Id"
                           class="btn btn-sm btn-danger">
                            Eliminar
                        </a>

                    </div>
                </td>
            </tr>
        }
    </tbody>
</table> *@