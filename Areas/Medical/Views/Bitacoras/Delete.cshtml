@* @model PIBitacoras.Models.Bitacora

@{
    ViewData["Title"] = "Eliminar bitácora";
}

<div class="container mt-4">

    <div class="card shadow border-0">

        <!-- HEADER -->
        <div class="card-header bg-danger text-white">
            <h4 class="mb-0">ELIMINAR BITÁCORA CLÍNICA</h4>
        </div>

        <!-- BODY -->
        <div class="card-body">

            <h5 class="mb-4">¿Seguro que deseas eliminar esta bitácora?</h5>

            <div class="row mb-3">

                <div class="col-md-4">
                    <strong>ID Alumno:</strong><br />
                    @Model.IdAlumno
                </div>

                <div class="col-md-4">
                    <strong>Motivo:</strong><br />
                    @Model.MotivoConsulta
                </div>

                <div class="col-md-4">
                    <strong>Fecha:</strong><br />
                    @Model.FechaHora.ToString("dd/MM/yyyy hh:mm tt")
                </div>

            </div>

            <div class="mb-3">
                <strong>Observaciones:</strong>
                <div class="bg-light p-3 rounded">
                    @Model.Observaciones
                </div>
            </div>

            <form asp-action="Delete" method="post">
                <input type="hidden" asp-for="Id" />

                <div class="d-flex justify-content-between mt-4">

                    <a asp-action="Index" class="btn btn-secondary px-4">
                        Cancelar
                    </a>

                    <button type="submit" class="btn btn-danger px-4">
                        Eliminar definitivamente
                    </button>

                </div>

            </form>

        </div>
    </div>
</div> *@