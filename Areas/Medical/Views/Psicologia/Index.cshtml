@* @model IEnumerable<PIBitacoras.ViewModels.PsicologiaListadoVM>

@{
    ViewData["Title"] = "Bitácoras Psicológicas";
}

<h2 class="mb-4">BITÁCORAS PSICOLÓGICAS</h2>

<a asp-action="Create" class="btn btn-dark mb-4">
    NUEVO REGISTRO PSICOLÓGICO
</a>

<table class="table table-hover align-middle">
    <thead class="table-light">
        <tr>
            <th>Matrícula</th>
            <th>Alumno</th>
            <th>Asistencia</th>
            <th>Fecha</th>
            <th>Folio</th>
            <th style="width:180px;">Acciones</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Matricula</td>
                <td>@item.NombreCompleto</td>

                <td>
                    @if (item.Asistencia == "Asistió")
                    {
                        <span class="badge bg-success">Asistió</span>
                    }
                    else if (item.Asistencia == "No asistió")
                    {
                        <span class="badge bg-danger">No asistió</span>
                    }
                    else
                    {
                        <span class="badge bg-secondary">@item.Asistencia</span>
                    }
                </td>

                <td>
                    @item.Fecha.ToString("dd/MM/yyyy hh:mm tt")
                </td>

                <td>
                    <strong>@item.Folio</strong>
                </td>

                <td>
                    <div class="d-flex gap-2">

                        <a asp-action="Details" asp-route-id="@item.Id"
                           class="btn btn-sm btn-primary">
                            Ver
                        </a>

                        <a asp-action="Edit" asp-route-id="@item.Id"
                           class="btn btn-sm btn-warning text-white">
                            Editar
                        </a>

                        <a asp-action="Delete" asp-route-id="@item.Id"
                           class="btn btn-sm btn-danger">
                            Eliminar
                        </a>

                    </div>
                </td>
            </tr>
        }
    </tbody>
</table> *@