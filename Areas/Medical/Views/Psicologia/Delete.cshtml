@* @model PIBitacoras.Models.Psicologia

@{
    ViewData["Title"] = "Eliminar registro";
}

<div class="container mt-4">

    <div class="card shadow border-0">

        <div class="card-header bg-danger text-white">
            <h4 class="mb-0">Eliminar registro psicológico</h4>
        </div>

        <div class="card-body">

            <h5 class="mb-4">¿Seguro que deseas eliminar este registro?</h5>

            <div class="row mb-3">

                <div class="col-md-6">
                    <strong>Folio:</strong> @Model.Fol
                </div>

                <div class="col-md-6">
                    <strong>Fecha:</strong>
                    @Model.AppointmentDatetime.ToString("dd/MM/yyyy hh:mm tt")
                </div>

            </div>

            <div class="mb-3">
                <strong>Asistencia:</strong> @Model.AttendanceStatus
            </div>

            <div class="mb-3">
                <strong>Observaciones:</strong>
                <div class="bg-light p-3 rounded">
                    @Model.PsychologyObservations
                </div>
            </div>

            <form asp-action="Delete" method="post">
                <input type="hidden" asp-for="Id" />

                <div class="d-flex justify-content-between mt-4">

                    <a asp-action="Index" class="btn btn-secondary px-4">
                        Cancelar
                    </a>

                    <button type="submit" class="btn btn-danger px-4">
                        Eliminar definitivamente
                    </button>

                </div>

            </form>

        </div>
    </div>

</div> *@