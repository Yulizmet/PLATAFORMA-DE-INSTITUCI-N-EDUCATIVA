@model SchoolManager.Areas.Grades.ViewModels.Subjects.SubjectViewModel

@{
    ViewData["Title"] = "Editar Materia";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<div class="container-fluid py-4">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="sistema-card">
                <div class="sistema-icon icon-calificaciones mb-4">
                    <i class="bi bi-pencil"></i>
                </div>

                <h2 class="users-title mb-4">Editar: @Model.Name</h2>

                <form asp-action="Edit" method="post">
                    <input type="hidden" asp-for="SubjectId" />
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert" style="border-left: 5px solid #D42D2D;"></div>

                    <div class="mb-4">
                        <label asp-for="Name" class="form-label fw-semibold" style="color: #1f2937;">Nombre de la Materia</label>
                        <input asp-for="Name" class="form-control"
                               style="border-radius: 10px; border: 1px solid #e0e0e0; padding: 12px;" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>

                    <div class="mb-4">
                        <label asp-for="GradeLevelId" class="form-label fw-semibold" style="color: #1f2937;">Nivel Escolar</label>
                        <select asp-for="GradeLevelId" class="form-select"
                                asp-items="@(new SelectList(ViewBag.GradeLevels, "GradeLevelId", "Name"))"
                                style="border-radius: 10px; border: 1px solid #e0e0e0; padding: 12px;">
                            <option value="">-- Selecciona un nivel --</option>
                        </select>
                        <span asp-validation-for="GradeLevelId" class="text-danger"></span>
                    </div>
                    <div class="mb-4">
                        <label asp-for="MinPassingGrade" class="form-label fw-semibold">Calificación mínima (opcional)</label>
                        <input asp-for="MinPassingGrade" class="form-control" type="number" step="0.5" min="0" max="10" />
                        <span asp-validation-for="MinPassingGrade" class="text-danger"></span>
                        <small class="form-text text-muted">Si se deja vacío, usará la del nivel escolar</small>
                    </div>

                    <div class="d-flex gap-2 justify-content-end">
                        <a asp-action="Index" asp-route-gradeLevelId="@Model.GradeLevelId" class="btn btn-secondary"
                           style="border-radius: 10px; background-color: #6c757d; color: white; padding: 10px 25px;">
                            <i class="bi bi-x-circle me-2"></i>Cancelar
                        </a>
                        <button type="submit" class="btn btn-calificaciones" style="padding: 10px 25px;">
                            <i class="bi bi-save me-2"></i>Guardar Cambios
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}